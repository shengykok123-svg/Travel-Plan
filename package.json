{
  "name": "travel-plan",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && wrangler d1 migrations apply travel-plan --local && wrangler pages dev public --binding DEV_MODE=1 --binding DEV_EMAIL=me@example.com",
    "deploy": "node scripts/build.mjs && wrangler d1 migrations apply travel-plan --remote && wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
